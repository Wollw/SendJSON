# SendJSON

Dalamud addon to send JSON strings to a specified server via macro.

[code]/sendjson[/code] to open the config window and [code]/sendjson command arg0 arg1 etc[/code] to trigger a command with arguments for placeholders in the with a \[#NUMBER#] format in the configured JSON string.
